# Copiar a .env (desarrollo) o .env.production. No commitear secretos.

# URL base del API Django (mismo host/puerto que runserver o el proxy objetivo)
# Ejemplo si Django corre en 8000:
VITE_API=http://127.0.0.1:8000
